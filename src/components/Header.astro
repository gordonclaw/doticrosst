---
const currentPath = Astro.url.pathname;

const navLinks = [
  { href: "/", label: "Home" },
  {
    label: "Services",
    children: [
      { href: "/services/", label: "All Services" },
      { href: "/services/website-design/", label: "Website Design & SEO" },
      { href: "/services/hair-barber/", label: "Hair & Barber Specialists" },
    ],
  },
  { href: "/about/", label: "About" },
  { href: "/contact/", label: "Contact" },
];

function isActive(href: string) {
  if (href === "/") return currentPath === "/";
  return currentPath.startsWith(href);
}
---

<header class="sticky top-0 z-50 border-b border-white/10 bg-charcoal/95 backdrop-blur-md">
  <div class="mx-auto flex h-18 max-w-7xl items-center justify-between px-4 sm:px-6 lg:px-8">
    <a href="/" class="flex items-center gap-2" aria-label="doticrosst home">
      <span class="text-xl font-light tracking-tight text-white">dot<strong class="font-extrabold text-accent-light">icrosst</strong></span>
    </a>

    <nav class="hidden items-center gap-1 md:flex" aria-label="Main navigation">
      {
        navLinks.map((link) =>
          link.children ? (
            <div class="relative group">
              <button
                class:list={[
                  "flex items-center gap-1 rounded-lg px-4 py-2 text-sm font-medium transition-colors duration-200",
                  currentPath.startsWith("/services")
                    ? "bg-white/10 text-white"
                    : "text-gray-300 hover:bg-white/10 hover:text-white",
                ]}
                aria-expanded="false"
                aria-haspopup="true"
              >
                {link.label}
                <svg width="12" height="12" viewBox="0 0 12 12" fill="none" class="transition-transform duration-200 group-hover:rotate-180" aria-hidden="true">
                  <path d="M3 5l3 3 3-3" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" />
                </svg>
              </button>
              <div class="invisible absolute left-0 top-full pt-2 opacity-0 transition-all duration-200 group-hover:visible group-hover:opacity-100">
                <div class="min-w-[220px] rounded-xl border border-white/10 bg-charcoal-light p-2 shadow-2xl">
                  {link.children.map((child) => (
                    <a
                      href={child.href}
                      class:list={[
                        "block rounded-lg px-4 py-2.5 text-sm transition-colors duration-200",
                        isActive(child.href)
                          ? "bg-accent/20 font-medium text-accent-light"
                          : "text-gray-300 hover:bg-white/10 hover:text-white",
                      ]}
                    >
                      {child.label}
                    </a>
                  ))}
                </div>
              </div>
            </div>
          ) : (
            <a
              href={link.href}
              class:list={[
                "rounded-lg px-4 py-2 text-sm font-medium transition-colors duration-200",
                isActive(link.href)
                  ? "bg-white/10 text-white"
                  : "text-gray-300 hover:bg-white/10 hover:text-white",
              ]}
            >
              {link.label}
            </a>
          )
        )
      }
    </nav>

    <div class="hidden items-center gap-3 md:flex">
      <a
        href="/contact/"
        class="rounded-lg bg-accent px-5 py-2.5 text-sm font-semibold text-white transition-all duration-200 hover:bg-accent-light hover:shadow-lg hover:shadow-accent/25"
      >
        Get Started
      </a>
    </div>

    <button class="flex flex-col gap-1.5 p-2 md:hidden" id="mobile-toggle" aria-label="Toggle menu" aria-expanded="false">
      <span class="block h-0.5 w-6 rounded bg-white transition-all duration-200"></span>
      <span class="block h-0.5 w-6 rounded bg-white transition-all duration-200"></span>
      <span class="block h-0.5 w-6 rounded bg-white transition-all duration-200"></span>
    </button>
  </div>

  <div class="hidden border-t border-white/10 bg-charcoal md:hidden" id="mobile-nav">
    <nav class="mx-auto max-w-7xl px-4 py-4 sm:px-6" aria-label="Mobile navigation">
      <div class="flex flex-col gap-1">
        {
          navLinks.map((link) =>
            link.children ? (
              link.children.map((child) => (
                <a
                  href={child.href}
                  class:list={[
                    "rounded-lg px-4 py-3 text-sm font-medium transition-colors duration-200",
                    isActive(child.href)
                      ? "bg-white/10 text-white"
                      : "text-gray-300 hover:bg-white/10 hover:text-white",
                  ]}
                >
                  {child.label}
                </a>
              ))
            ) : (
              <a
                href={link.href}
                class:list={[
                  "rounded-lg px-4 py-3 text-sm font-medium transition-colors duration-200",
                  isActive(link.href)
                    ? "bg-white/10 text-white"
                    : "text-gray-300 hover:bg-white/10 hover:text-white",
                ]}
              >
                {link.label}
              </a>
            )
          )
        }
        <a
          href="/contact/"
          class="mt-2 rounded-lg bg-accent px-5 py-3 text-center text-sm font-semibold text-white transition-all duration-200 hover:bg-accent-light"
        >
          Get Started
        </a>
      </div>
    </nav>
  </div>
</header>

<script>
  const toggle = document.getElementById("mobile-toggle") as HTMLButtonElement;
  const nav = document.getElementById("mobile-nav") as HTMLElement;
  const bars = toggle?.querySelectorAll("span");

  toggle?.addEventListener("click", () => {
    const open = toggle.getAttribute("aria-expanded") === "true";
    toggle.setAttribute("aria-expanded", String(!open));
    nav.classList.toggle("hidden");
    if (bars) {
      bars[0].classList.toggle("translate-y-2");
      bars[0].classList.toggle("rotate-45");
      bars[1].classList.toggle("opacity-0");
      bars[2].classList.toggle("-translate-y-2");
      bars[2].classList.toggle("-rotate-45");
    }
  });
</script>
